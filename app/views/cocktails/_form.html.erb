<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name, placeholder: "Mojito" %>
  <%= f.input :picture, placeholder: "URL of cocktail picture" %>
  <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
